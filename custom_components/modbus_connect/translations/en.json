{
  "config": {
    "step": {
      "user": {
        "title": "Gateway connection",
        "data": {
          "host": "Host",
          "port": "Port",
          "slave_id": "Slave / unit ID",
          "prefix": "Device name (optional, defaults to manufacturer + model)"
        }
      },
      "device": {
        "title": "Device type",
        "data": {
          "filename": "Device configuration file"
        }
      }
    },
    "abort": {
      "already_configured": "This gateway + slave ID is already configured.",
      "no_device_files": "No device configuration files found."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Modbus Connect options",
        "data": {
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}
